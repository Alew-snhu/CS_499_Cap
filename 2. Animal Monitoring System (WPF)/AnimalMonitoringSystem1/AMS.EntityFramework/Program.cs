using System;

namespace AMS.EntityFramework
{
    class Program
    {
                                                    
        static void Main(string[] args)
        {
            // database seeder was called to fill database with default values on creation.

            //var context = new AmsDbContext();

            //AMSDbContextSeeder.Seed(context);
        }
    }
}
